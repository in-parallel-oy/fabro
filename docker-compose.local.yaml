services:
  fabro:
    image: in-parallel-oy/fabro
    restart: unless-stopped
    ports:
      - "32276:32276"
    volumes:
      - fabro-storage:/storage
      - /var/run/docker.sock:/var/run/docker.sock

volumes:
  fabro-storage:
